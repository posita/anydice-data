EVASION: 10

function: attackroll ROLL:n with BONUS:n dealing DIE:n die SIZE:n plus FLATBONUS:n {
 if ROLL+BONUS >= 10+EVASION { result: (2*DIE)dSIZE + 2*FLATBONUS}
 else if ROLL+BONUS >= EVASION { result: DIEdSIZE + FLATBONUS }
 else { result: 0 }
}

output [attackroll 1d20 with 2 dealing 2 die 6 plus 4]