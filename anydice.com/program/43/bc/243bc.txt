MISS: 0
HIT: 1
CRITICALHIT: 2

function: attacka ROLL:n vs DEFENSE:n {
 if ROLL = 1 { result: MISS }
 if ROLL = 20 {
  if ROLL + FIRSTATTACK >= DEFENSE { result: CRITICALHIT }
  result: HIT
 }
 if ROLL + FIRSTATTACK - 10 >= DEFENSE { result: CRITICALHIT }
 if ROLL + FIRSTATTACK >= DEFENSE { result: HIT }
 result: MISS
}

function: attackb ROLL:n vs DEFENSE:n {
 if ROLL = 1 { result: MISS }
 if ROLL = 20 {
  if ROLL + SECONDATTACK >= DEFENSE { result: CRITICALHIT }
  result: HIT
 }
 if ROLL + SECONDATTACK - 10 >= DEFENSE { result: CRITICALHIT }
 if ROLL + SECONDATTACK >= DEFENSE { result: HIT }
 result: MISS
}

function: attackc ROLL:n vs DEFENSE:n {
 if ROLL = 1 { result: MISS }
 if ROLL = 20 {
  if ROLL + THIRDATTACK >= DEFENSE { result: CRITICALHIT }
  result: HIT
 }
 if ROLL + THIRDATTACK - 10 >= DEFENSE { result: CRITICALHIT }
 if ROLL + THIRDATTACK >= DEFENSE { result: HIT }
 result: MISS
}

function: damage for ATTACKARESULT:n {
 if ATTACKARESULT = CRITICALHIT {
  result: CRITICALDAMAGE
 }
 if ATTACKARESULT = HIT {
  result: HITDAMAGE
 }
 result: MISSDAMAGE
}
 

function: attacka {
 result: [damage for [attacka d20 vs DEFENSE]]
}
function: attackb {
 result: [damage for [attackb d20 vs DEFENSE]]
}
function: attackc {
 result: [damage for [attackc d20 vs DEFENSE]]
}

DEFENSE: d{15..25}

FIRSTATTACK: 10
SECONDATTACK: 5
THIRDATTACK: 0

DICE:4
STR:4
A:0
B:0
C:0

HITDAMAGE: DICEd12+STR
CRITICALDAMAGE: ((DICEd12+STR)*2)
MISSDAMAGE: 0
output [attacka] named "Greataxea"
output [attackb] named "Greataxeb"
output [attackc] named "Greataxec"

output 0

HITDAMAGE: DICEd10+STR
CRITICALDAMAGE: ((DICEd10+STR)*2)
MISSDAMAGE: 0
output [attacka] named "Falchiona"

HITDAMAGE: DICEd10+STR+DICE
CRITICALDAMAGE: ((DICEd10+STR+DICE)*2)
MISSDAMAGE: 0
output [attackb] named "Falchionb"

HITDAMAGE: DICEd10+STR+DICE+DICE
CRITICALDAMAGE: ((DICEd10+STR+DICE+DICE)*2)
MISSDAMAGE: 0
output [attackc] named "Falchionc"

HITDAMAGE: DICEd10+STR+DICE+DICE
CRITICALDAMAGE: ((DICEd10+STR+DICE+DICE)*2)
MISSDAMAGE: 0
output [attackb] named "Falchionq"

output 1

FIRSTATTACK: 10
SECONDATTACK: 5
THIRDATTACK: 0

DICE:4
STR:4
A:0
B:0
C:0

HITDAMAGE: DICEd12+STR
CRITICALDAMAGE: (2*DICE)d12+(STR*2)
MISSDAMAGE: 0
output [attacka] named "Greataxea"
output [attackb] named "Greataxeb"
output [attackc] named "Greataxec"

output 0

HITDAMAGE: DICEd10+STR
CRITICALDAMAGE: (2*DICE)d10+(STR*2)
MISSDAMAGE: 0
output [attacka] named "Falchiona"

HITDAMAGE: DICEd10+STR+DICE
CRITICALDAMAGE: (2*DICE)d10+((STR+DICE)*2)
MISSDAMAGE: 0
output [attackb] named "Falchionb"

HITDAMAGE: DICEd10+STR+DICE+DICE
CRITICALDAMAGE: (2*DICE)d10+((STR+DICE+DICE)*2)
MISSDAMAGE: 0
output [attackc] named "Falchionc"

HITDAMAGE: DICEd10+STR+DICE+DICE
CRITICALDAMAGE: (2*DICE)d10+((STR+DICE+DICE)*2)
MISSDAMAGE: 0
output [attackb] named "Falchionq"
