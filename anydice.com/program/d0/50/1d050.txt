output d4 named "d4, not exploded"

loop DEPTH over {1..6} {
 set "explode depth" to DEPTH
 output [explode 1 d4] named "d4, exploded at most [DEPTH] times on 4"
}

function: explodeone ROLLEDVALUE:n {
 if ROLLEDVALUE = 1 { result: 1 + [explodeone d4] }
 result: ROLLEDVALUE
}

loop DEPTH over {1..6} {
 set "maximum function depth" to DEPTH
 output [explodeone d4] named "d4, exploded at most [DEPTH] times on 1"
}