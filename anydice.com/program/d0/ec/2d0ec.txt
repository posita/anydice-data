function: interpret ROLL:s {
 if [count 6 in ROLL] { result: 1 } \ success \
 if [count 5 in ROLL] { result: 1 } \ success \
 if [count 4 in ROLL] { result: 1 } \ success \
 if [count 3 in ROLL] { result: -1 } \ failure \
 if [count 2 in ROLL] { result: -1 } \ failure \
 if [count 1 in ROLL] { result: -1 } \ failure \
}

loop DICE over {1..2} {
 output [interpret DICE d6] named "[DICE]d"
}