function: interpret ROLL:s {
ONES: [count 1 in ROLL]
SIX: [count 6 in ROLL]
 if SIX { result: SIX } \ success \
 if ONES { result: ONES*-1 } \ failure \
 result: 0 \ guided event \
}

loop DICE over {1..10} {
 output [interpret DICE d6] named "[DICE]d"
}