function: evaluate ROLL:s with TN:n {
  SUCCESSES: ROLL >= TN
  if SUCCESSES > 0 { result: SUCCESSES }
  if [count 1 in ROLL] { result: -1 }
  result: 0
}
  TN: 3
  loop DICE over { 1..10 }{
    output [evaluate DICE d10 with TN] named "[DICE]d"
  }
