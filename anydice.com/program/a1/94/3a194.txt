TARGET: 4

function: interpret ROLL:s {
 if [count 1 in ROLL] { result: [count 1 in ROLL]* -1 } \ failure \
 result: [count {TARGET..6} in ROLL] \ successes \
}

loop DICE over {1..10} {
 output [interpret DICE d6] named "[DICE]d"
}