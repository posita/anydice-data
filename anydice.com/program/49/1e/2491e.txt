function: count highest dupes of SEQ:s in DICE:s {
  MAX: 0
  loop X over SEQ {
    if (X = DICE) > 1 & (X = DICE) > MAX { MAX: X = DICE }
  }
  result: MAX
}

output [count highest dupes of {2,3,4,5,6} in 5d6]