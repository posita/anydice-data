function: interpret ROLL:s {
 if [count 3 | 4 | 5 | 6 in ROLL] { result: 1 } \ success \
 if [count 1 | 2 in ROLL] { result: -1 } \ failure \
}

loop DICE over {1..10} {
 output [interpret DICE d6] named "[DICE]d"
}