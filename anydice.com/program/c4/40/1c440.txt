\ Usage, scroll down to output:                                    \
\ [b X](4+) will roll X black dice.                                \
\ [be X] will roll and explode X black dice.                       \
\ [br X] will roll X black dice and reroll failures.               \
\ Similarly, [g X](3+) and [w X](2+) and related functions exist   \
\ for other dice.[d X](5+)                                         \
\ The dice can be added together, e.g. [br 3] + [wr 2]             \

set "maximum function depth" to 7 
\do not set this below 4, as wrapper function count against the limit\

function: roll ROLL:n threshold T {
  if ROLL >=T {result: 1}
  result: 0
}

function: rollexploding ROLL:n threshold T{
  if ROLL =6 {result: 1+[rollexploding 1d6 threshold T]}
  result: [roll ROLL threshold T]
}

function: rerollfailures ROLL:n threshold T{
  if ROLL < T {result: [roll 1d6 threshold T]}
  result: [roll ROLL threshold T]
}

function: rerollthenexplode ROLL: n threshold T {
  if ROLL < T {result: [rollexploding 1d6 threshold T]}
  result: [rollexploding ROLL threshold T]
}

function: crit ROLL: n threshold T {
  if ROLL = 6 {result: 2}
  result: [roll ROLL threshold T]
}

function: rerollthencrit ROLL: n threshold T {
  if ROLL < T {result: [crit 1d6 threshold T]}
  result: [roll ROLL threshold T]
}

function: wrapper DICE:n threshold T explode E reroll R crit S{
  RES:0
  loop N over {1..DICE} {
    if E & R {RES: RES+[rerollthenexplode 1d6 threshold T]}
    else {if R & S {RES: RES+[rerollthencrit 1d6 threshold T]}
     else {if E {RES:RES+[rollexploding 1d6 threshold T]}
      else {if R {RES:RES+[rerollfailures 1d6 threshold T]}
       else {if S {RES:RES+[crit 1d6 threshold T]}
        else {RES:RES+[roll 1d6 threshold T]}
        }
       }
      }
     }
    }
  result:RES
  }

function: dd DICE:n{
  result:[wrapper DICE threshold 5 explode 0 reroll 0 crit 0]
}
function: dde DICE:n{
  result:[wrapper DICE threshold 5 explode 1 reroll 0 crit 0]
}
function: ddr DICE:n{
  result:[wrapper DICE threshold 5 explode 0 reroll 1 crit 0]
}
function: dds DICE:n{
  result:[wrapper DICE threshold 5 explode 0 reroll 0 crit 1]
}
function: ddre DICE:n{
  result:[wrapper DICE threshold 5 explode 1 reroll 1 crit 0]
}

function: ddrs DICE:n{
  result:[wrapper DICE threshold 5 explode 0 reroll 1 crit 1]
}

function: b DICE:n{
  result:[wrapper DICE threshold 4 explode 0 reroll 0 crit 0]
}
function: be DICE:n{
  result:[wrapper DICE threshold 4 explode 1 reroll 0 crit 0]
}
function: br DICE:n{
  result:[wrapper DICE threshold 4 explode 0 reroll 1 crit 0]
}
function: bs DICE:n{
  result:[wrapper DICE threshold 4 explode 0 reroll 0 crit 1]
}
function: bre DICE:n{
  result:[wrapper DICE threshold 4 explode 1 reroll 1 crit 0]
}

function: brs DICE:n{
  result:[wrapper DICE threshold 4 explode 0 reroll 1 crit 1]
}

function: g DICE:n{
  result:[wrapper DICE threshold 3 explode 0 reroll 0 crit 0]
}
function: ge DICE:n{
  result:[wrapper DICE threshold 3 explode 1 reroll 0 crit 0]
}
function: gr DICE:n{
  result:[wrapper DICE threshold 3 explode 0 reroll 1 crit 0]
}
function: gs DICE:n{
  result:[wrapper DICE threshold 3 explode 0 reroll 0 crit 1]
}
function: gre DICE:n{
  result:[wrapper DICE threshold 3 explode 1 reroll 1 crit 0]
}

function: grs DICE:n{
  result:[wrapper DICE threshold 3 explode 0 reroll 1 crit 1]
}

function: w DICE:n{
  result:[wrapper DICE threshold 2 explode 0 reroll 0 crit 0]
}
function: we DICE:n{
  result:[wrapper DICE threshold 2 explode 1 reroll 0 crit 0]
}
function: wr DICE:n{
  result:[wrapper DICE threshold 2 explode 0 reroll 1 crit 0]
}
function: ws DICE:n{
  result:[wrapper DICE threshold 2 explode 0 reroll 0 crit 1]
}
function: wre DICE:n{
  result:[wrapper DICE threshold 2 explode 1 reroll 1 crit 0]
}

function: wrs DICE:n{
  result:[wrapper DICE threshold 2 explode 0 reroll 1 crit 1]
}

output [bs 3] named "default"

