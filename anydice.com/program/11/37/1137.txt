D:{1,2,2}

function: compare A:n to B:n {
 if A = B { result: 0 }
 if A < B { result:-1 }
 if A > B { result: 1 }
}

function: comparereroll A:n to B:n {
 if A < B { result:-1 }
 if A > B { result: 1 }
}

output [compare dD to dD] named "compare dD to dD"
output [comparereroll dD to dD] named "comparereroll dD to dD"