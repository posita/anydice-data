function: SEQUENCE:s contains at least NUMBER:n pairs {
 result: (SEQUENCE = 1) >= NUMBER & (SEQUENCE = 2) >= NUMBER
}

loop N over {1..8}{
loop P over {1..4}{
output [Nd{1,1,1,2,2,3} contains at least P pairs] named "[N]d6, [P]pairs min"
}
}