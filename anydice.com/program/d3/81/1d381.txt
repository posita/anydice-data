function: evaluate ROLL:s {
 FAILURES: ROLL <= 2
 SUCCESSES: ROLL >= 7

 if FAILURES > 5 { result: SUCCESSES + (ROLL = 10) - 5 }
 else { result: SUCCESSES + (ROLL = 10) - FAILURES }
}

loop DICE over {1..6} {
 output [evaluate DICE d10] named "[DICE]d"
}