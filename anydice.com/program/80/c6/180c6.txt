function: evaluate ROLL:s {
 SUCCESSES: ROLL <= 15
 if SUCCESSES > 0 { result: SUCCESSES + (ROLL <= 3) }
 if [count 1 in ROLL] { result: -1 }
 result: 0
}

loop DICE over {1..5
} {
 output [evaluate DICE d20] named "[DICE]d"
}