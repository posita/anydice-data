function: limit X:n to LB:n and UB:n {
  if X < LB { result: LB }
  if X > UB { result: UB }
  result: X
}

loop DL over {1..2} {
  loop BONUS over {0,2,4} {
    output [limit ((2d6+BONUS)/4-(DL-1)) to 0 and 999] named "+[BONUS] against difficulty level [DL]"
  }
}