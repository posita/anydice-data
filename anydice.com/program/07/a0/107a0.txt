function: degats minimums de X:n et Y:n et Z:n et B:n degats bonus contre caracteristique C:n {
 if (X + Y + Z) > C { result: 0 }
 if X = Y { 
  if X = Z { result : X + Y + Z + B }
  if X < Z { result : X + Y + B }
  result : Z + B
 }
 if X < Y {
  if X = Z { result : X + Z + B }
  if X < Z { result : X + B }
  result : Z + B
 }
 if X > Y {
  if Y = Z { result : Y + Z + B }
  if Y < Z { result : Y + B }
  result : Z + B
 }
}

function: degats moyens de X:n et Y:n et Z:n et B:n degats bonus contre caracteristique C:n {
 if (X + Y + Z) > C { result: 0 }
 if X = Y { 
  if X = Z { result : X + Y + Z + B }
  result : X + Y + B
 }
 if X < Y {
  if X = Z { result : X + Z + B }
  if X < Z {
   if Y = Z { result : Y + Z + B }
   if Y < Z { result : Y + B }
   result : Z + B
  }
  result : X + B
 }
 if X > Y {
  if X = Z { result : X + Z + B }
  if X < Z { result : X + B }
  if X > Z {
   if Y = Z { result : Y + Z + B }
   if Y < Z { result : Z + B }
   result : Y + B
  }
 }
}

function: degats maximums de X:n et Y:n et Z:n et B:n degats bonus contre caracteristique C:n {
 if (X + Y + Z) > C { result: 0 }
 if X = Y { 
  if X = Z { result : X + Y + Z + B }
  if X < Z { result : Z + B }
  result : X + Y + B
 }
 if X < Y {
  if Y = Z { result : Y + Z + B }
  if Y < Z { result : Z + B }
  result : Y + B
 }
 if X > Y {
  if X = Z { result : X + Z + B }
  if X < Z { result : Z + B }
  result : X + B
 }
}

output [degats minimums de 1d6 et 1d6 et 1d12 et 0 degats bonus contre caracteristique 15] named "2d6 + 1d12 + 0 < 15"
output [degats moyens de 1d6 et 1d6 et 1d12 et 0 degats bonus contre caracteristique 15] named "2d6 + 1d12 + 0 < 15"
output [degats maximums de 1d6 et 1d6 et 1d12 et 0 degats bonus contre caracteristique 15] named "2d6 + 1d12 + 0 < 15"