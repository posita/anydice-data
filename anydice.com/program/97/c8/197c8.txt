function: reroll DIE:d if under LIMIT:n {
  result: [reroll DIE as DIE if under LIMIT]
}
function: reroll ROLL:n as DIE:d if under LIMIT:n {
  if ROLL < LIMIT { result: DIE }
  else { result: ROLL }
}
function: adept DIE:d if under LIMIT:n {
  result: [adept DIE as DIE if under LIMIT]
}
function: adept ROLL:n as DIE:d if under LIMIT:n {
  if ROLL < LIMIT { result: LIMIT }
  else { result: ROLL }
}

output 18d12+42 named "finn + magical striker and martial artist @lv 20"
output 48d[reroll d6 if under 3]+8d8+8*8 named "Olog full send lv 20"
