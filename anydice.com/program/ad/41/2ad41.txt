function: dice DICE:n skill SKILL:n spec SPEC:n  {
 if SPEC > 2 {
  DICE: DICE + 1
  SPEC: 2
 }

 ROLL: DICEd10
 S: [count {1..SKILL} in ROLL]
 if SPEC > 0 {
  S: S + [count {1..SPEC} in ROLL]
 }
 result: S
}
output [dice 4 skill 5 spec 3]
output [dice 5 skill 5 spec 2]

