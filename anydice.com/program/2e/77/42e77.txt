function: ROLL:n or REROLL:d if over MAX:n {
  if ROLL > MAX { result: REROLL }
  else { result: ROLL }
}

loop MAX_ROLL over {4,3,2} {
  DIE: [d6 or 0 if over MAX_ROLL]

  output [1dDIE or 3 if over 3] named "d6, rolls over [MAX_ROLL] count as 0, cap sum at 3"
  output [2dDIE or 3 if over 3] named "2d6, rolls over [MAX_ROLL] count as 0, cap sum at 3" 
}
