function: ROLL:n or REROLL:d if over MAX:n {
  if ROLL > MAX { result: REROLL }
  else { result: ROLL }
}

DIE: [d6 or 0 if over 4]

output [1dDIE or 3 if over 3] named "Favorable"
output [2dDIE or 3 if over 3] named "Favorable Pushed" 

function: ROLL:n or REROLL:d if over MAX:n {
  if ROLL > MAX { result: REROLL }
  else { result: ROLL }
}

DIE: [d6 or 0 if over 3]

output DIE named "Fair"
output [2dDIE or 3 if over 3] named "Fair Pushed"

function: ROLL:n or REROLL:d if over MAX:n {
  if ROLL > MAX { result: REROLL }
  else { result: ROLL }
}

DIE: [d6 or 0 if over 2]

output DIE named "Unfavorable"
output [2dDIE or 3 if over 3] named "Unfavorable Pushed" 