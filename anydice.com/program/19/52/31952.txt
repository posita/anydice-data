function: reroll N:n {
 if N < 5 { result: 0 + [reroll d6] }
 result: N
}

loop REROLLS over {1..3}{
  set "maximum function depth" to REROLLS
  SUCCESSES: [count {5, 6} in 5d[reroll d6]]

  REROLLNUMBER : REROLLS - 1
  output SUCCESSES named "Successes at [REROLLNUMBER] ReRolls"
}