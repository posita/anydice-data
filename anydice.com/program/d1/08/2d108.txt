set "explode depth" to 3

function: sev N:n plus X:n {
  result: [explode dN+X>=N]
}

loop DIE over {4,6,8,10,12} {
  loop NUM over {1,2,3,4}{
    output NUMd[sev DIE plus 0]+1 named "Severity ([NUM]d[DIE])"
  }
}