output 1d100 named "Heaven’s Dice"

function: if A:n is above B test if C:n is above D {
 if A > B {if C > D {result: C }
 result: 1
}
 result: 0
}

output [if d100 is above 90 test if d100 is above 80] named "Obsession Roll 90"
output [if d100 is above 80 test if d100 is above 80] named "Obsession Roll 80"
output [if d100 is above 70 test if d100 is above 80] named "Obsession Roll 70"
output [if d100 is above 60 test if d100 is above 80] named "Obsession Roll 60"
output [if d100 is above 50 test if d100 is above 80] named "Obsession Roll 50"
output [if d100 is above 40 test if d100 is above 80] named "Obsession Roll 40"
output [if d100 is above 30 test if d100 is above 80] named "Obsession Roll 30"
output [if d100 is above 20 test if d100 is above 80] named "Obsession Roll 20"
output [if d100 is above 10 test if d100 is above 80] named "Obsession Roll 10"
output [if d100 is above 00 test if d100 is above 80] named "Obsession Roll 00"