CARD: 1
CMOD: 0
RMOD: 0
DIF: [highest 1 of 1d6]
ROLL: [highest 1 of 1d6]

output ((CARD + CMOD) < DIF) & ((ROLL + RMOD) < DIF) named "Miss"

output (((CARD + CMOD) >= DIF) & ((ROLL + RMOD) < DIF)) | (((CARD + CMOD) < DIF) & ((ROLL + RMOD) >= DIF)) named "Weak Hit"

output ((CARD + CMOD) >= DIF) & ((ROLL + RMOD) >= DIF) named "Strong Hit"