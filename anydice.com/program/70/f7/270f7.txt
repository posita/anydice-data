function: roll ROLL:n plus BONUS:n vs TARGET:n with REROLL:d reroll {
  SUM: ROLL + BONUS
   \Only reroll if less than N\
  if SUM >= TARGET {
    result: ROLL + BONUS
  } else {
    result: REROLL + BONUS
  }
}

output [roll 2d6 plus 1 vs 7 with 2d6 reroll] named "Roll 2D6+1, reroll if under 7"