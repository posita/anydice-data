
set "maximum function depth" to 8

function: POOL:n pool DEPTH:n {
 if (POOL > 0) {
  X: [POOLd6 remaining]
  Y: DEPTH+1
  result: [X pool Y]
 }
 result: DEPTH
}

function: ROLL:s remaining {
 X: (ROLL > 3)
 result: X

}

output [4 pool 0] named "# of rolls to empty a pool of 4"
output [6 pool 0] named "# of rolls to empty a pool of 6"
output [8 pool 0] named "# of rolls to empty a pool of 9"

