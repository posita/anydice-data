function: interpret ROLL:n for STAT {
 if ROLL <= STAT / 2 { result: 2 } \ success \
 if ROLL <= STAT { result: 1 } \ mixed \
 result: 0 \ failure \
}

loop STAT over {1..20} {
 output [interpret d20 for STAT] named "[STAT]"
}
