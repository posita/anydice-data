function: reroll ROLL:s as REROLL:d if less than MIN:n {
  if ROLL >= MIN {result: ROLL} else {result: REROLL}
}

output [reroll d8 as d8 if less than 2] named "reroll if one"
output d8 named "Default"

