function: fate4_filtered() { var results = []; for (var i = 0; i < 4; i++) { results.push(roll(1, 5)); } // Filter out results that are less than 0 or greater than 4. results = results.filter(result => result >= 0 && result <= 4); // Return the filtered results. return results; } // Generate 100 results. fate4_filtered();
