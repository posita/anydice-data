T: 2                \ variables are uppercase, get assigned a value with colon \ 
S: {3, 1, 2, 4}  \ sequeces differ from dice in that the order is fixed \
U: {1, 2, 3, 4}
output #S named "len"       \# is unary length op, elements for seq, # dice count 3d6 => 3, numbers => digit count\
output S
output 2 * S   named "formula"  \ all values are summed as if sequence was a single number for math ops \
output S > 2   named "vs bool"  \ all values are compared and 1s are summed for bolean comparators \
output S > d3  named "vs die"   \ die is compared to sum of numbers in seq \
output S = U   named "vs seq"   \ each value is compared, ? \
A: d{1:1, 2:3, 3:1} \ a die variable - first number is value, second is count, comment between backslashes \
B: d{1, 2, 2, 2, 3} \ same - can leave off counts if they are all 1 \
X: d{d3, 4..6 }     \ d generates a sequence, .. does too\
output X named "complicated way to do a d6" \ can have names to be shown as title\
L: dT               \ unary dice op creates a sequence of all values at equal prop \
output d2 + d3      \ in formulas, operator gets applied to each value of 1st dice vs each value of 2nd dice \

loop N over {L} { \ curlies to enclose code blocks; loop syntax is "loop VAR over SEQ, which can be generated by die\
   output NdB = 3 named "round [N]"  \ comparators = != < > <= >= return 0 if false or 1 if true , use [X] for var in string\
}

function: double N:n { \function: defines a function; uppercase varnames in function definition \
   result: N * 2 \ return is called result: ; math operators - + / * ^  boolean ops & | ! unary dice operator d \
}

function: trouble N:n { if N < 3 { result: 0 } else { result: 3 }}

output [double [trouble S]] named "double trouble" \ call funcitons by encolsing them with params in [] \