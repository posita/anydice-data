function: reroll lowest of ROLL:s as REROLL:d if less than MIN:n {
  LOWEST: (#ROLL)@ROLL       \ the lowest die is sorted last \
  if LOWEST >= MIN {
    result: ROLL + 0
  } else {
    REST: {1..#ROLL-1}@ROLL  \ all but the lowest die \
    result: REST + REROLL
  }
}

output 10d6 + 5d6 named "5ray + hex"
output [reroll lowest of 10d6 as d6 if less than 2]+5d6 named "5ray+phle+hex"

output 9d6 named "fireball"
output [reroll lowest of 9d6 as d6 if less than 2] named "fireball+phle"

output 5d8 named "wall of fire"
output [reroll lowest of 5d8 as d8 if less than 2] named "wall of fire + phle"

