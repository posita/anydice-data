set "explode depth" to 1

\ the above line determines how many times the die explode \

function: explode N:n {
  if N = 0 {
    result: [explode d{0,1,2}] + 1 = 0
  }
  result: N
}

\ N is what number the die explodes on, this function declares if: 0 is rolled, explode dice else:keep result \

output [explode d{1 = 0,1,2}] named "5D3"

\ to change how many die you want just put a number before the lowercase d \

output [explode 5d{0,1,2}]

