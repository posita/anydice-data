function: check ROLLA:n vs ROLLB:n vs ROLLC:n vs STAT {
 if ROLLA <= STAT & ROLLB <= STAT {
  if ROLLA <= STAT & ROLLB <= STAT & (ROLLC = ROLLA | ROLLC = ROLLB ) {
   result: 2
  }
  result: 1
 }
 if ROLLA <= STAT & ROLLB > STAT {
  result: 0
 }
 if ROLLA > STAT & ROLLB > STAT {
  if ROLLA > STAT & ROLLB > STAT & (ROLLC = ROLLA | ROLLC = ROLLB ) {
    result: -2
  }
  result: -1
 }
}

output [check 1d20 vs 1d20 vs 1d20 vs 1]

