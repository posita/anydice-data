function: check ROLLA:n vs ROLLB:n vs ROLLC:n vs TARGET {
 if ROLLA <= TARGET & ROLLB <= TARGET {
  if ROLLA <= TARGET & ROLLB <= TARGET & (ROLLC = ROLLA | ROLLC = ROLLB ) {
   result: 2
  }
  result: 1
 }
 if (ROLLA <= TARGET & ROLLB > TARGET | ROLLA > TARGET & ROLLB <= TARGET) {
  result: 0
 }
 if ROLLA > TARGET & ROLLB > TARGET {
  if ROLLA > TARGET & ROLLB > TARGET & (ROLLC = ROLLA | ROLLC = ROLLB ) {
    result: -2
  }
  result: -1
 }
}

output [check 1d20 vs 1d20 vs 1d20 vs 10]

output 1d20