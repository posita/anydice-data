function: two highest tie for DIESIZE:n {
 result: [highest or tie for 2dDIESIZE]
}

function: highest or tie for ROLL:s {
 if 1@ROLL = 2@ROLL { result: DIESIZE }
 result: 1@ROLL
}

function: reroll N:n once below THRESHOLD:n {
 if N < THRESHOLD { result: d20 }
 result: N
}

function: reroll always below THRESHOLD:n {
 result: d{THRESHOLD..20}
}

Y: 6
X: 6

output [reroll d20 once below dX] named "reroll if below, once"
output [reroll always below dX] named "reroll if below, until high enough"

T: [two highest tie for Y]

output T named "highest with tie"

output [reroll d20 once below T] named "reroll if below, once, combined"
output [reroll always below T] named "reroll if below, until high enough, combined"
