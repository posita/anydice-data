FAIL: -1         \Fail the check, no information\
PARTSUCCESS: 0   \Fail, but less than 5\
SUCCESS: 1
HIGH: 15         \Difficulty of identify test\
LOW: HIGH - 5    \Fail by 5 or more\
BONUS: 5         \The accumulated bonus to the roll\
ABMOD: 5

function: save ROLL:n {
 if ROLL <= LOW { result: FAIL }
 if ROLL >= HIGH { result: SUCCESS  }
 result: PARTSUCCESS
}

loop ABMOD over {-2..5}{ 
\Between the {} either enter a number or BONUS (in all caps) 
     to loop through a range of values enter either a string of
     comma seperated numbers (e.g. 0,2,4,6), or a range of values (e.g. 5..15)\

output [save 3d6 + ABMOD + BONUS] named "Diff. [HIGH] Test + [ABMOD]" }
