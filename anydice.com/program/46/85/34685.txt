function: if CONDITION:n then A:n else B {
  if CONDITION { result: A } else { result: B }
}

function: replace lowest NUMBER:n of DICE:d with REROLLS:d if below THRESHOLD:n {
 LOW: [lowest NUMBER of DICE]
 result: [if LOW<THRESHOLD then DICE - LOW + REROLLS else DICE]
}

output [replace lowest 1 of 2d10 with 1d10 if below 5]