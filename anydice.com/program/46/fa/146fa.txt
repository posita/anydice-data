function: ROLL:n vs DIF:n {
 if ROLL >= TOTAL & ROLL >= TOTAL { result: [threat DICE] }
 if ROLL >= TOTAL | ROLL = TOTAL { result: DICE}
 result: 0
}

function: threat ROLL:n {
 if ROLL >= TOTAL | ROLL = TOTAL { result: DICE}
 result: DICE
}

TOTAL: 6
DICE: d6


output [DICE vs TOTAL]