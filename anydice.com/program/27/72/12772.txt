function: tell ROLL:s vs TARGET:n {
  HIT: ROLL = 1
  loop TN over { 2..TARGET } {
    HIT: HIT + (ROLL = TN)*TN
  }
  result: [ highest 2 of HIT ]
}

loop POOL over { -2..3 } {
  output (POOL+3) d{1,2,3,0,0,0} + POOL named "Atribute [POOL]"
}