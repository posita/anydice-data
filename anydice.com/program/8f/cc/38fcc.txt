function: compare A:n or B:n against C:n {
  L: [lowest of A and B]
  H: [highest of A and B]
  if H <= C & L <= C & H = L { result: 3}
  if H <= C & L <= C { result: 2}
  if L <= C { result: 1 }
  if L > C & H = L { result: -1 }
  result: 0
}
loop X over {1..12}{
 output [compare d12 or d12 against X] named "TN: [X]"
}