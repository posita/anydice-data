function: keep A:n if under B:n {
 if A <= B { result: A }
 result: 0
}

output [keep d20 if under 3]-[keep d20 if under 9]>0
output [keep d20 if under 3]-[keep d20 if under 9]<0

output [keep d20 if under 7]-[keep d20 if under 7]>0
output [keep d20 if under 7]-[keep d20 if under 7]<0

output [keep d20 if under 8]-[keep d20 if under 12]>0
output [keep d20 if under 8]-[keep d20 if under 12]<0

output [keep d20 if under 12]-[keep d20 if under 16]>0
output [keep d20 if under 12]-[keep d20 if under 16]<0