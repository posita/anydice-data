function: compare A:n or B:n against C:n {
  L: [lowest of A and B]
  H: [highest of A and B]
  if H <= C & L <= C & H = L { result: 2}
  if H <= C & L <= C { result: 1}
  if L <= C { result: 0 }
  if L > C & H = L { result: -2 }
  result: -1
}
loop X over {1..12}{
 output [compare d12 or d12 against X] named "TN: [X]"
}