function: reroll lowest of ROLL:s as REROLL:d if less than MIN:n {
  LOWEST: (#ROLL)@ROLL       \ the lowest die is sorted last \
  if LOWEST >= MIN {
    result: ROLL + 0
  } else {
    REST: {1..#ROLL-1}@ROLL  \ all but the lowest die \
    result: REST + REROLL
  }
}

function: rerolll lowest of ROLL:s as REROLL:d if total less than MIN:n {
  LOWEST: (#ROLL)@ROLL       \ the lowest die is sorted last \
  if ROLL >= MIN {
    result: ROLL + 0
  } else {
    REST: {1..#ROLL-1}@ROLL  \ all but the lowest die \
    result: REST + REROLL
  }
}


output [reroll lowest of 2d6 as d6 if less than 4] named "2d6 replace lowest if < 4"
output [rerolll lowest of 2d6 as d6 if less than 10] named "2d6 replace lowest if < 10"
output 2d6