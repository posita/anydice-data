function: evaluate ROLL:s {
 SUCCESSES: {{[count 10 in ROLL] >= 2} | {[count 9 in ROLL] >= 2}} 
 if SUCCESSES > 0 { result: 1 }
 result: 0
}

loop DICE over {1..8} {
 output [evaluate DICE d10] named "[DICE]d"
}