function: reroll ROLL:n at SIZE:n on REROLL:n {
 if (ROLL<=REROLL) {
  result: REROLL
 }
 result: ROLL
}

DICE_SIZE: 6

loop N over {0..(DICE_SIZE - 1)}{
 output [reroll dDICE_SIZE at DICE_SIZE on N] named "reroll [N] or less"
}