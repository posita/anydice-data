function: X:n if at most Y:n else Z:n {
  if X <= Y { result: X }
  else { result: Z }
}

L: 7  \ highest failing roll \

loop N over {1..10} {  \ total number of dice in pool \
  if N = 1 { K: 0 } else { K: 1 }  \ minimum number of successes needed \

  X: [Nd(d12 <= L) if at most N-K else 0]
  output XdL / 4 named "(sum of [N]d12 less than [L]) / 4 with at least [K] successes"
}