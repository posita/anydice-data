loop TARGET over {2..10} {
  loop LEVEL over {-3..3} {
    if (LEVEL >= 0) { \ Advantage or neutral \
      output 1@(LEVEL+1)d(d10>=TARGET)named "[LEVEL]v[TARGET]"
    } else { \ Disadvantage \
      output (-(LEVEL-1))@(-(LEVEL-1))d(d10>=TARGET) named "[LEVEL]v[TARGET]"
    }
  }
}
