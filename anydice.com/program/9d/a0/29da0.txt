function: count highest dupes in DICE:s {
 MAX: 0
 loop X over {1..1@DICE} {
   COUNT: X = DICE
   if COUNT > 1 & COUNT > MAX { MAX: COUNT }
 }
 result: MAX
}

loop POOL over {1..10} {
 output [count highest dupes in POOLd6] named "Threat/Momentum [POOL]d6"
}
