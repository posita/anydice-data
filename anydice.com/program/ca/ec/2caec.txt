

function: clamp n N:n min MIN:n max MAX:n {
  if (N < MIN) {
     result: MIN
  }
  else if (N > MAX) {
     result: MAX
  }
  else {
     result: N
  }
}

loop DICE over {2..10} {
   output [clamp n (DICE)d{0,0,1} min 1 max 2] named "[DICE] dice"
}