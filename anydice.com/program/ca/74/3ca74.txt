function:  roll DICE:n withbonus BONUS:n against DC:n {
 
  CRITBY: DICE + BONUS - DC
  result: (DICE + BONUS) + (CRITBY >= 0) * (CRITBY + BONUS)
}

output [roll 1d100 withbonus 16 against 50] + [roll 1d100 withbonus 16 against 50] + 261 + 25 >= 500
