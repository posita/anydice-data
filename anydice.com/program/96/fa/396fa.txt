function: count highest dupes in DICE:s {
  MAX: 0
  loop X over {1..1@DICE} {
    COUNT: X = DICE
    if (COUNT*X) > MAX { MAX: COUNT*X }
  }
  result: MAX
}

loop SKILL over {1..5}
{
output [count highest dupes in (SKILL)d4] named "Rank [SKILL]"
}