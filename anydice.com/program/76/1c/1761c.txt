set "position order" to "highest first"

function: pool ROLL:s { result: 1@ROLL } 
function: drag ROLL:s { result: 1@ROLL } 
\rolling normal dice\
\rolling negation dice\


loop A over {1..6} { \how many normal dice\
loop B over {0..3} { \how many negation dice\

  function: D:s negate P:s {
    loop Y over {1..#(D)} {
      if [count D in P] = #P{
        result: 0
\how many pool=drag, if equal to # of pool\
\then everything negated already\


      } else if [D contains P] {
        C: 0
        loop X over {1..#P} {
          if C = 0{
            if [D contains X@P] {
            } else {
              C: C + X
            }
          } else {
            result: C@P
          }
        }


      } else {
        result: 1@P
      }
    }
  } 
\if negation = normal, 0. otherwise, result is normal\

  output [[drag Bd6] negate [pool Ad6]] named "[A]d -- [B] Drag"
}
}
