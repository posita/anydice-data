function: pb L:n {
 result: ((L - 1) / 4) +2
}

function: md L:n {
 ML: [pb L]
 if ML < 5 {result: 1 d (ML*2)}
 else {result: 1d10 + (ML - 4)}
}

loop CL over {1,5,9,13,17}{
 output [md CL] named "md [CL]"
 output [pb CL] named "pb [CL]"
}