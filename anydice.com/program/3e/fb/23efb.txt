function: evaluate ROLL:s with RR:n rerolls success on WIN:n or more {
 SUCCESSES: ROLL >= WIN
 FAILURES: ROLL < WIN
 if (FAILURES > 0) {
  if RR > FAILURES { 
   result: SUCCESSES + (RR - (RR - FAILURES) d6 >= WIN) }
   else { result: SUCCESSES + (RR d6 >= WIN) }
  }
 if SUCCESSES > 0 {
  result: SUCCESSES
 }
 result: 0
}

loop DICE over {1..5} {
 output [evaluate DICE d6 with 2 rerolls success on 5 or more] named "[DICE]d"
}





