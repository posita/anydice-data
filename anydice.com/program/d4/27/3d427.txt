loop REALM over {0..9} {
 loop T over {5} {
  POOL: T + 6 + 10*(REALM)
  VAL: 10 - T + REALM
  output POOL named "base R[REALM] T[T]"
 }
}

loop REALM over {0..9} {
 loop T over {5} {
  POOL: T + 6 + 10*(REALM)+ (2*REALM)d4
  VAL: 10 - T + REALM
  output POOL named "d4 R[REALM] T[T]"
 }
}

loop REALM over {0..9} {
 loop T over {5} {
  POOL: T + 6 + 10*(REALM)+ (2*REALM)d12
  VAL: 10 - T + REALM
  output POOL named "d8 R[REALM] T[T]"
 }
}