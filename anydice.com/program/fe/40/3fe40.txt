loop N over {1..20} {
  \ A pool success (1) is when the number of 6s rolled is 1 or more \
  POOL_SUCCESS: (N d (d6 = 6)) >= 1

  \ Add the two pool results (0 or 1) together \
  RESULT: POOL_SUCCESS + POOL_SUCCESS

  output RESULT named "[N] dice"
}
