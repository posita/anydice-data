set "position order" to "lowest first"

R: {1,2}@3d6
N: {1,3}@3d6
S: {2,3}@3d6
L: 8
M: 10
H: 12

output R + 1 >= L named "risky position + ability 1 vs low stake"
output R + 1 >= M named "risky position + ability 1 vs normal stake"
output R + 1 >= H named "risky position + ability 1 vs high stake"

output R + 2 >= L named "risky position + ability 2 vs low stake"
output R + 2 >= M named "risky position + ability 2 vs normal stake"
output R + 2 >= H named "risky position + ability 2 vs high stake"

output R + 3 >= L named "risky position + ability 3 vs low stake"
output R + 3 >= M named "risky position + ability 3 vs normal stake"
output R + 3 >= H named "risky position + ability 3 vs high stake"

output R + 4 >= L named "risky position + ability 4 vs low stake"
output R + 4 >= M named "risky position + ability 4 vs normal stake"
output R + 4 >= H named "risky position + ability 4 vs high stake"

output N + 1 >= L named "normal position + ability 1 vs low stake"
output N + 1 >= M named "normal position + ability 1 vs normal stake"
output N + 1 >= H named "normal position + ability 1 vs high stake"

output N + 2 >= L named "normal position + ability 2 vs low stake"
output N + 2 >= M named "normal position + ability 2 vs normal stake"
output N + 2 >= H named "normal position + ability 2 vs high stake"

output N + 3 >= L named "normal position + ability 3 vs low stake"
output N + 3 >= M named "normal position + ability 3 vs normal stake"
output N + 3 >= H named "normal position + ability 3 vs high stake"

output N + 4 >= L named "normal position + ability 4 vs low stake"
output N + 4 >= M named "normal position + ability 4 vs normal stake"
output N + 4 >= H named "normal position + ability 4 vs high stake"


output S + 1 >= L named "safe position + ability 1 vs low stake"
output S + 1 >= M named "safe position + ability 1 vs normal stake"
output S + 1 >= H named "safe position + ability 1 vs high stake"

output S + 2 >= L named "safe position + ability 2 vs low stake"
output S + 2 >= M named "safe position + ability 2 vs normal stake"
output S + 2 >= H named "safe position + ability 2 vs high stake"

output S + 3 >= L named "safe position + ability 3 vs low stake"
output S + 3 >= M named "safe position + ability 3 vs normal stake"
output S + 3 >= H named "safe position + ability 3 vs high stake"

output S + 4 >= L named "safe position + ability 4 vs low stake"
output S + 4 >= M named "safe position + ability 4 vs normal stake"
output S + 4 >= H named "safe position + ability 4 vs high stake"