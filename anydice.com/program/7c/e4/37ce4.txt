function: reroll greatest of ROLL:s as REROLL:n if greater than TARGET:n {
   if 1@ROLL > TARGET {
      result: (ROLL <= TARGET) + (REROLL <= TARGET) -1
   } else {
      result: 0
   }
}
function: beat a TARGET:n percent chance in THROWS:n throws using a DICE:d {
 result: [beat a TARGET percent chance in THROWS throws using a DICE with [maximum of DICE] faces]
}

function: beat a TARGET:n percent chance in THROWS:n throws using a DICE:n with FACES:n faces {
 if THROWS = 0 {
   result: (1dFACES <= (TARGET / 2))
 }

 if DICE > TARGET {
  result: [beat a TARGET percent chance in THROWS-1 throws using a 1dFACES with FACES faces]
 } else {
  result: 1
 } 
}

loop N over {2, 10, 18, 25, 50, 75} {
 output [beat a N percent chance in 1 throws using a 1d100] named "target: [N]" 
} 
