set "position order" to "highest first"

function: countset SEQUENCE:s {
 SORTEDSEQUENCE : [sort SEQUENCE]
 COUNT: 0
 loop P over {1..#SEQUENCE} {
  COUNT: COUNT + (P@SORTEDSEQUENCE = 1@SORTEDSEQUENCE)
 }
 result: COUNT
}
output [countset {2, 2, 5, 5, 5, 4}]