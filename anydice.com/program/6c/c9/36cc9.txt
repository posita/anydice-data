DADO: {1,0,0,2,3,2,3,2}

function: at least one blank stella in ROLL:s {
  result: [count 0 in ROLL] >= 1
}
function: at least two blank stella in ROLL:s {
  result: [count 0 in ROLL] >= 2
}
function: at least one luna in ROLL:s {
  result: [count 1 in ROLL] >= 1
}
function: at least two luna in ROLL:s {
  result: [count 1 in ROLL] >= 2
}
function: at least one sole in ROLL:s {
  result: [count 2 in ROLL] >= 1
}
function: at least two sole in ROLL:s {
  result: [count 2 in ROLL] >= 2
}


loop N over {1..8} {
output [at least one blank stella in NdDADO] named "At least 1 BLANK/STELLA with [N]d8 ->" }

loop N over {1..8} {
if N > 1 {
 output [at least two blank stella in NdDADO] named "At least 2 BLANK/STELLA with [N]d8 ->" }}

loop N over {1..8} {
output [at least one luna in NdDADO] named "At least 1 LUNA ............. with [N]d8->" }

loop N over {1..8} {
if N > 1 {
 output [at least two luna in NdDADO] named "At least 2 LUNA ............. with [N]d8->" }}

loop N over {1..8} {
output [at least one sole in NdDADO] named "At least 1 SOLE ............. with [N]d8->" }

loop N over {1..8} {
if N > 1 {
output [at least two sole in NdDADO] named "At least 2 SOLE ............. with [N]d8->" }}