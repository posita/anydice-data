function: chances of hitting exactly X:n identical dice in SEQ:s at least once {
  HIGH: 0
  loop N over { SEQ } {
    if X = HIGH { result: 1 }
    if (SEQ = N) > HIGH { HIGH: (SEQ = N) }
  }
  result: 0 }

output [chances of hitting exactly 3 identical dice in 3d6 at least once]