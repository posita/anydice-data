function: reroll ROLL:s as REROLL:d if more than MIN:n {
  if ROLL <= MIN {
    result: ROLL + 0
  } else {
    result: [reroll REROLL as REROLL if more than MIN]
  }
}

output [reroll d12 as d12 if more than 11] - 1
