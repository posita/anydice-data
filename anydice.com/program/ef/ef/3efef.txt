TN:10

\what if first dice, the "primary dice", is the "dominant dice" and so you roll that dice twice?\

loop FIRSTDICE over {2,4,6}{
 loop SECONDDICE over {2,4,6}{
  output 2d(FIRSTDICE) + d(SECONDDICE) named "[FIRSTDICE]::[SECONDDICE]"
}
}