function: interpret ROLL:s {
 if [count 6 in ROLL] { result: 1 } \ success \
 if [count 
5 in ROLL] { result: 2 } \ failure \ if [count 4 in ROLL] { result: 2 }
 result: 0 \ guided event \
}

loop DICE over {1..10} {
 output [interpret DICE d6] named "[DICE]d"
}