function: clamp N:n between MIN:n and MAX:n {
  if N > MAX { result: MAX }
  if N < MIN { result: MIN }
  result: N
}

RANGE: 25

DIE: d{0..5}
MIN: 0
MAX: [maximum of DIE]

function: roll {
  result: [roll DIE]
}
function: roll N:n {
  if N = MIN { result: N - [explode DIE] }
  if N = MAX { result: N + [explode DIE] }
  result: N
}

set "explode depth" to 10

output [clamp [explode DIE]-[explode DIE] between -RANGE and RANGE] named "z5! vs"
output [clamp [roll]-3 between -RANGE and RANGE] named "roll z5!"