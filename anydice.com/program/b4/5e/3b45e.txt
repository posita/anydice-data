function: interpret ROLL:s vs CR:s {
if ([count 2 in ROLL]+[count 3 in ROLL] >= [count 2 in CR]+[count 3 in CR]+2) { result: 2 } \crit\ 
if ([count 2 in ROLL]+[count 3 in ROLL] > [count 2 in CR]+[count 3 in CR]) \& ([count 1 in ROLL]<[count 3 in ROLL])\ { result: 1 } \ hit\
if ([count 2 in ROLL]+[count 3 in ROLL] = [count 2 in CR]+[count 3 in CR]) { result: 0 } \mid\
if ([count 2 in ROLL]+[count 3 in ROLL]+2 <= [count 2 in CR]+[count 3 in CR]) { result: -2} \crit fail\ 
 else { result: -1 } \ fail \ 
}

loop DICE over {1..5} {
 loop CHAL over {1..5}{
 output [interpret DICE d4 vs CHAL d4] named "[DICE]d [CHAL]"
}
}