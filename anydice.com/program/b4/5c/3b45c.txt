function: interpret ROLL:s vs CR:s {
 if ([count 2 in ROLL]+[count 3 in ROLL] > [count 2 in CR]+[count 3 in CR]) & ([count 2 in ROLL]<[count 3 in ROLL]) { result: 2 } \ crit\
 if ([count 2 in ROLL]+[count 3 in ROLL] > [count 2 in CR]+[count 3 in CR]) & ([count 2 in ROLL]>=[count 3 in ROLL]) { result: 0 } \ hit\
 if ([count 2 in ROLL]+[count 3 in ROLL] < [count 2 in CR]+[count 3 in CR]) & ([count 2 in CR]>=[count 3 in CR]) { result: 0 } \ hit\
 if ([count 2 in ROLL]+[count 3 in ROLL] = [count 2 in CR]+[count 3 in CR]) { result: 0 } \ mid\
 else { result: -1 } \ fail \
}

loop DICE over {3..6} {
 loop CHAL over {3..6}{
 output [interpret DICE d3 vs CHAL d3] named "[DICE]d [CHAL]"
}
}