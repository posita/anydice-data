function: ROLL_ONE:s ROLL_TWO:s but replace low result below two {
 if (ROLL_ONE < 2) { result: ROLL_TWO + 1d6 }
 else if (ROLL_TWO < 2) { result: ROLL_ONE + 1d6 }
 else { result: ROLL_ONE + ROLL_TWO } 
}

function: ROLL_ONE:s ROLL_TWO:s but replace low result below three {
 if (ROLL_ONE < 3) { result: ROLL_TWO + 1d6 }
 else if (ROLL_TWO < 3) { result: ROLL_ONE + 1d6 }
 else { result: ROLL_ONE + ROLL_TWO } 
}

function: ROLL_ONE:s ROLL_TWO:s but replace low result below four {
 if (ROLL_ONE < 4) { result: ROLL_TWO + 1d6 }
 else if (ROLL_TWO < 4) { result: ROLL_ONE + 1d6 }
 else { result: ROLL_ONE + ROLL_TWO } 
}

function: ROLL_ONE:s ROLL_TWO:s but replace low result below five {
 if (ROLL_ONE < 5) { result: ROLL_TWO + 1d6 }
 else if (ROLL_TWO < 5) { result: ROLL_ONE + 1d6 }
 else { result: ROLL_ONE + ROLL_TWO } 
}

function: ROLL_ONE:s ROLL_TWO:s ROLL_THREE:s but replace low result below three with sneak {
 if (ROLL_ONE < 3) { result: ROLL_TWO + ROLL_THREE + 1d6 }
 else if (ROLL_TWO < 3) { result: ROLL_ONE + ROLL_THREE + 1d6 }
 else if (ROLL_THREE < 3) { result: ROLL_ONE + ROLL_TWO + 1d6 }
 else { result: ROLL_ONE + ROLL_TWO + ROLL_THREE } 
}

function: ROLL_ONE:s ROLL_TWO:s ROLL_THREE:s but replace low result below four with sneak {
 if (ROLL_ONE < 4) { result: ROLL_TWO + ROLL_THREE + 1d6 }
 else if (ROLL_TWO < 4) { result: ROLL_ONE + ROLL_THREE + 1d6 }
 else if (ROLL_THREE < 4) { result: ROLL_ONE + ROLL_TWO + 1d6 }
 else { result: ROLL_ONE + ROLL_TWO + ROLL_THREE } 
}



function: HIT_ROLL_ONE:s HIT_ROLL_TWO:s ROLL_ONE:s ROLL_TWO:s ROLL_CRIT:s danno atteso sneak attack contro ac AC:n {
 if (HIT_ROLL_ONE = 20) | (HIT_ROLL_TWO = 20) {
  
 }
 else if ((HIT_ROLL_ONE+7) >= AC) | ((HIT_ROLL_TWO+7) >= AC) { 
  if (ROLL_ONE < 3) & (ROLL_ONE <= ROLL_TWO) { result: ROLL_TWO + 1d6 } 
  else if (ROLL_TWO < 3) & (ROLL_TWO <= ROLL_ONE) { result: ROLL_ONE + 1d6 }
  else { result: ROLL_ONE + ROLL_TWO } }
 else { result: 0 }
}

function: HIT_ROLL_ONE:s HIT_ROLL_TWO:s ROLL_ONE:s ROLL_TWO:s ROLL_THREE:s optimized  {
 if (ROLL_ONE < 3) { result: ROLL_TWO + ROLL_THREE + 1d6 }
 else if (ROLL_TWO < 3) { result: ROLL_ONE + ROLL_THREE + 1d6 }
 else if (ROLL_THREE < 3) { result: ROLL_ONE + ROLL_TWO + 1d6 }
 else { result: ROLL_ONE + ROLL_TWO + ROLL_THREE }
}


output [1d6 1d6 but replace low result below three] named "2d6 reroll 1,2"
output [1d20 1d20 1d6 1d6 1d6 danno atteso sneak attack contro ac 18] named "Sneak attack ottimizzato"

