function: count POOL:s at least TARGET {
 result: POOL >= TARGET
}

function: total POOL:s at least TARGET {
 SUCCESSES: POOL >= TARGET
 result: SUCCESSES * TARGET
}

loop TARGET over {1..6} {
 output [total 3d6 at least TARGET] named "target [TARGET]"
}