function: kind COUNT:n on DICE:s {
  COUNT_KINDS: 0
  loop VALUE over {1..s} {
    if [count VALUE in DICE] >= COUNT { COUNT_KINDS: COUNT_KINDS + 1 }
  }
  result: COUNT_KINDS >= COUNT
}

output [kind 5 on 5d6]
