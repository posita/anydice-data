function: minmax ROLL:n between MAX:n and MIN:n {
  if ROLL > MAX {
    result: MAX
  } else if ROLL < MIN {
    result: MIN
  } else {
    result: ROLL
  }
}

loop DIFFICULTY over {5..11} {
  loop SKILL over {2..5} {
    output [minmax SKILLd(d12 > DIFFICULTY) between 2 and 0] named "[SKILL]d12 > [DIFFICULTY]"
  }
}