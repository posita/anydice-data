THROW: [highest 3 of 4d{2..6}]
function: reroll ROLL:n if below MINIMUM:n {
if ROLL >= MINIMUM { result: ROLL }
}
loop MINIMUM over {7} {
output [reroll THROW if below MINIMUM] named "rerolling if below [MINIMUM]"
}