TARGET: 2

function: interpret ROLL:s {
 if [count {1+TARGET..6} in ROLL] >= TARGET { result: 1 } \ success \
 if [count {1..TARGET} in ROLL] = DICE { result: -1 } \ failure \
 result: 0 \ guided event \
}

loop DICE over {1..10} {
 output [interpret DICE d6] named "[DICE]d"
}