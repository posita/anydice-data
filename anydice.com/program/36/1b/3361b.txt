function: evaluate ROLL:s {
 SUCCESSES: ROLL >= 5
 if SUCCESSES > 0 { result: SUCCESSES + (ROLL = 10) }
 if [count 8 in ROLL] {explode d8}
 result: 0
}

loop DICE over {1..10} {
 output [evaluate DICE d8] named "[DICE]d"
}