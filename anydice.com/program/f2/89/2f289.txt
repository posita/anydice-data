function: reroll N:n {
 if N < 5 { result: N + [reroll d6] }
 result: N
}

loop REROLLS over {1..3}{
  REROLLNUMBER : REROLLS - 1
  set "maximum function depth" to REROLLS
  output [count {5, 6} in 5d[reroll d6]] named "Successes at [REROLLNUMBER] ReRolls"
}