function: interpret ROLL:s vs CR:s {
 if ([count 2 in ROLL]+[count 3 in ROLL] >= CR) & ([count 2 in ROLL]<[count 3 in ROLL]) { result: 1 } \ success \
 if ([count 2 in ROLL]+[count 3 in ROLL] >= CR) & ([count 2 in ROLL]>=[count 3 in ROLL]){ result: 0 } \ medium \
 else { result: -1 } \ fail \
}

loop DICE over {1..6} {
 loop CHAL over {1..6}{
 output [interpret DICE d3 vs CHAL] named "[DICE]d [CHAL]"
}
}