function: attack ROLL:n and R:n and RR:n vs AC:n {
 if HIT >= AC {
  if ROLL = 20 {result: BC}
   result: HIT}
 if ROLL = 20 {result: CRIT}
 if ROLL = 1 {
  if RR < R{result: RR}
  result: R {

   result:MISS}
 if ROLL + HIT >= AC {result: HIT}
 if ROLL + HIT < AC {
  if {result: HIT}
   result: MISS}
}
