
function:get X:n plus A:n but substract if zero{
if A = 0 {
result: (X-1)
}
else{
result: (X+A)
}
}

function: roll X:n dice Y:n and count max values{
A:0
loop N over {1..X}{
A:A+(1dY>=Y)
}
result:A
}

X:6

A:0

loop N over {1..3}{
A: A + (1d4>=4)
}

A:[roll 3 dice 4 and count max values]

output [roll [get X plus A but substract if zero] dice 8 and count max values] named "Slots Spin's Jackpot"
output ([get X plus A but substract if zero]d8) named "Slots Spin's damage"

output [roll 1 dice 14 and count max values] + [roll 6 dice 8 and count max values] named "Stacked Deck's Jackpot"
