set "maximum function depth" to 10

function: successes on A threshold B again C {
  result: Ad[success d10 threshold B again C helper]
}

function: success A:n threshold B again C helper {
  if A >= C { result: 1 + [successes on 1 threshold B again C] }
  else {
    if A >= B { result: 1 }
    else { result : 0 }
  }
}

output [successes on 6 threshold 8 again 10]

