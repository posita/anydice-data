function: ROLL:s under U:n {
  TOTAL: 0  
  loop RES over {1..#ROLL} {
    if RES@ROLL < U { TOTAL: TOTAL + RES@ROLL }
    else { }
  }
  result: TOTAL
}

function: ROLL:s over O:n {
  TOTAL: 0  
  loop RES over {1..#ROLL} {
    if RES@ROLL > O { TOTAL: TOTAL + RES@ROLL }
    else { }
  }
  result: TOTAL
}

output [1d10 under 8]
output [1d6 over 2]
