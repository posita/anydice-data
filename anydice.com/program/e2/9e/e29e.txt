function : interpret ROLL:s MOD:n {
    SUCCESS : 0
    TMOD : MOD
    RROLL : [sort ROLL]
    loop I over {1 .. #RROLL} {
        TMOD : I@RROLL + TMOD - 6
        if TMOD >= 0 {SUCCESS : SUCCESS + 1}
    }
    result : SUCCESS
}

loop I over {0 .. 10} { output [interpret 6d6 I] named "6d6 + [I] modifier"}

