function: critical SUM:n{
 if SUM >= 1000{
  TENS: SUM / 1000
  CRIT: TENS / 2
  SUM : SUM - (TENS * 999)
  result: CRIT * 2 + SUM
 }
 result: SUM
}

loop DIFF over {1..10}{
 loop DICE over {1..10}{
  output [critical DICEd{0:5,1:4,1000}] >= DIFF named "Difficulty [DIFF] [DICE] dice "
 }
}