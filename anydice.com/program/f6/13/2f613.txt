

function: count highest matches in DICE:s {
  MAX: 0
  loop X over {1..1@DICE} {
    COUNT: X = DICE
    if COUNT > 1 & COUNT > MAX { MAX: COUNT }
  }
  result: MAX
}

output [count highest matches in 9d9] named "most matches in hand of 9 of 9 categories"
