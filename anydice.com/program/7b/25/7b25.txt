function: hob POOL:n DICE:n {
   ROLL: DICE d 6
   if ROLL < 10 {
       result: 0
   }
   result: 1 + (POOL-DICE)
}

loop POOL over { 1..5 } {
   loop DICE over { 1..POOL } {
       output [hob POOL DICE] named "[DICE] out of [POOL]"
   }
}