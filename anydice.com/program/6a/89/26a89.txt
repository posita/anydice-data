function: rm MODIFIER:n {
  result: [roll 1d10 vs 1d10]
}

function: roll R:n vs B:n {
   if R = B {result: 3 * (R - 2 * (R / 2)) - 1}
   result: (B + MODIFIER) > R
}

loop BONUS over {0..10} {
   output [rm BONUS] named "+[BONUS]"
}