STATICSUCCESS: 13
MAINDICE: 1d10
MAXSTATIC: -2
MINSTATIC: 2

function: critical success for A:n and B:n {
 if A = B & (A = 2 | A = 4 | A = 6 | A = 8 | A = 10 | A = 12) { result: 1 }
 result: 0
}

function: critical fail for A:n and B:n {
 if A = B & (A = 1 | A = 3 | A = 5 | A = 7 | A = 9 | A = 11) { result: 1 }
 result: 0
}

function: critical success for simple A and B {
 if A = B & (A = 2 | A = 4 | A = 6 | A = 8 | A = 10 | A = 12) { result: 1 }
 result: 0
}

function: critical fail for simple A and B {
 if A = B & (A = 1 | A = 3 | A = 5 | A = 7 | A = 9 | A = 11) { result: 1 }
 result: 0
}

function: evaluate non static A:n and B:n {
 if (A + B) <= SUCCESS & [critical fail for simple A and B] = 0 { result: 1 }
 result: 0
}

function: evaluate static A:n and B:n {
 if (A + B) <= STATICSUCCESS & [critical fail for simple A and B] = 0 { result: 1 }
 result: 0
}

function: evaluate with max static A:n and B:n {
 if (A + B + MAXSTATIC) <= STATICSUCCESS { result: 1 }
 result: 0
}

function: evaluate with min static A:n and B:n {
 if (A + B + MINSTATIC) <= STATICSUCCESS { result: 1 }
 result: 0
}

output [evaluate static 1d4 and MAINDICE] named "1d4 SUCCESS"

output [evaluate static 1d6 and MAINDICE] named "1d6 SUCCESS"

output [evaluate static 1d8 and MAINDICE] named "1d8 SUCCESS"

output [evaluate static 1d10 and MAINDICE] named "1d10 SUCCESS"

output [evaluate static 1d12 and MAINDICE] named "1d12 SUCCESS"