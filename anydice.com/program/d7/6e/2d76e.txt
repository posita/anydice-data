function: clamp N:n between MIN:n and MAX:n {
  if N > MAX { result: MAX }
  if N < MIN { result: MIN }
  result: N
}

RANGE: 30
D: 20
MID: D/2 + 1
MAX: [maximum of D]
ZNINE: {0..9}
ZFIVE: {0..5}

function: extend N:n {
  if N = MAX { result: N - 1 + [extend dD] }
  result: N
}

output [clamp [extend dD]-MID between -RANGE and RANGE] named "d[D]!-[MID]"
output [clamp [extend dD]-[extend dD] between -RANGE and RANGE] named "d[D]! vs"
