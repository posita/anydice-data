

function: eval ROLL:s {
 if ([count 1 in ROLL] > 1) { result: -4 }
 else {
   result: [count {4..12} in ROLL] - [count 1 in ROLL]
 }
}

loop DICE over {1..3} {
 loop SIDES over {4,6,8,10,12} {
  output [eval DICE d SIDES] named "[DICE]d[SIDES]"
 }
}