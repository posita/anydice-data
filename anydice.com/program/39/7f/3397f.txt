function: artistry math DICE:n roll DICES:n BONUS DC {
 if DICE+BONUS >= DC {
 if DICES+BONUS >= DC {result:(DICE+DICES+BONUS+BONUS)*DC}
 else {result: (DICE+BONUS)*DC}
 }
 else {if DICES+BONUS >= DC {result: (DICES+BONUS)*DC}
 else {result: 0}
 }
}
output [artistry math d20 roll d20 25 30]