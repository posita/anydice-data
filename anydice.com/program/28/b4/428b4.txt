function: A:n abaixo de B:n max C:n{
 if A<=B{
  if B>C{result:A+[DADO abaixo de 2*A-B-C max C]}
  result:A+[DADO abaixo de 2*A-B max C]}
 result:0
}

set "maximum function depth" to 2
DADO:d6

loop X over {1..25}{
 output [DADO abaixo de X max 20] named "[X]"
}