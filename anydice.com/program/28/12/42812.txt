function: check ROLL:n under UNDER:n over OVER:n {
  if (ROLL <= UNDER) & (ROLL > OVER) { result: 1 }
  else { result: 0 }
}

loop UNDER over {1..20} {
  loop OVER over {1..20} {
    output [check 1d20 under UNDER over OVER] named "Under [UNDER] and Over [OVER]"
  }
}