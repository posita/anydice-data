set "maximum function depth" to 8

function: explode DIE:d {
 result: [explode DIE helper] \have to call another function bc can't boolean dice\
}

function: explode N:n helper {
 if N = 1 { result: [explode DIE helper] + [explode DIE helper] } \if I rolled a 1, then return two more dice with explosion applied to them\
 result: N \otherwise just return the rolled value\
}

output [explode 1d6]