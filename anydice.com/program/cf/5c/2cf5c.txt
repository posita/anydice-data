set "explode depth" to 3
function: if CONDITION:n then A else B {
  if CONDITION { result: A } else { result: B }
}
function: roll SIDE:n DICE:s {
   ROLL:[if (2@DICE >= 6) then (DICE + [explode 1dSIDE]) else DICE]
   result: ROLL
}
output [roll 6 3d6] 