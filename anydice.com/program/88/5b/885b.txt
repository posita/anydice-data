

\Basic Attacks in 1 roll combat system\
\Attack & Defender have no modifiers from traits or items\

N: 2d6

function: semiexplode N:n {
 if  N = 12 {result:12 + d6}
 else {result: N}
}

HANDICAPW: [semiexplode N+3] >= ([semiexplode N]+1+3-3-3)
HANDICAPM: [semiexplode N+3] >= ([semiexplode N]+1+6-3-3)
HANDICAPS: [semiexplode N+3] >= ([semiexplode N]+1+9-3-3)
EXECUTEW: [semiexplode N+3] >= ([semiexplode N]+2+3-3-3)
EXECUTEM: [semiexplode N+3] >= ([semiexplode N]+2+6-3-3)
EXECUTES: [semiexplode N+3] >= ([semiexplode N]+2+9-3-3)
KILL: [semiexplode N+3] >= ([semiexplode N]+2+12-3-3)

output HANDICAPW named "Weak Handicap"
output HANDICAPM named "Mid Handicap"
output HANDICAPS named "Strong Handicap"
output EXECUTEW named "Weak Execute"
output EXECUTEM named "Mid Execute"
output EXECUTES named "Strong Execute"
output KILL named "InstaDeath"

\Obviously Defender is too strong.. lets change the goal mods from +3 and +5 to +1 and +2\
\Somewhat better... but defender is still too strong... lets assume most weapons give at least +3\
\Better, much better. Small chance of failure is fine... lets see how impairs effect defense? (Add -3 to defender)\
\What about a double impair? (Add -3 to defender again)\
