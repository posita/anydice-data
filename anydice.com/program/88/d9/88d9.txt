DIETYPE: d6
MIN: 1
MAX: 4

function: roll FIRST:n against SECOND:n with values X:n and Y:n {
  if FIRST > SECOND { result: 1 }
  if FIRST < SECOND { result: -1}

  if FIRST = SECOND { 
    if X = Y {result: 0}

    if X > Y {result: 1}
    if X < Y {result: -1}
  }

}

output 0 named "Förlust = -1, lika = 0, vinst = 1"

loop X over {MIN..MAX}{
  loop Y over {MIN..MAX}{
    output [roll [count {4..6} in XdDIETYPE] against [count {4..6} in YdDIETYPE] with values X and Y] named "Vinst [X] vs [Y]"
  }

  output 0 named "~~~~~~~~~~~"
}