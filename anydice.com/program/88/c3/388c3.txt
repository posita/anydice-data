function: explode-or-fail ROLL:n MAX:n {
  if ROLL = 1 { result: 1 }  # If reroll is 1, total becomes 1 (failure)
  if ROLL = MAX { result: MAX + [explode-or-fail dMAX MAX] }  # If max value, explode and reroll
  result: ROLL  # Otherwise, just return the roll result
}

output [explode-or-fail d6 6] + [explode-or-fail d8 8]
