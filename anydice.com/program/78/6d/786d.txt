function: DICE:d reroll under MIN:n {
 result: [DICE DICE reroll under MIN]
}

function: DICE:d ROLL:n reroll under MIN:n {
 if ROLL < MIN {
  result: [DICE DICE reroll under MIN]
 }
 result: ROLL
}

set "maximum function depth" to 10

output [[highest 1 of 2d20] reroll under 2]