function: dice for rank N {
 result: 2 + (2 * N)
}

loop ATTR over {1..5}{
 loop ABIL over {1..5}{
  DATTR: [dice for rank ATTR]
  DABIL: [dice for rank ABIL]
  output dDATTR + dDABIL named "d[DATTR] + d[DABIL]"
 }
}