function: reroll lowest of ROLL:s as REROLL:d if less than MIN:n or smaller than ALL:n {
  loop N over ROLL {
    LOWEST : (#ROLL)@ROLL
    if (LOWEST >= MIN) {
      
  }
  if LOWEST >= MIN {
    result: ROLL + 0
  } else {
    REST: {1..#ROLL-1}@ROLL  \ all but the lowest die \
    result: REST + REROLL
  }
}
output [reroll lowest of 2d12 as d12 if less than 7]